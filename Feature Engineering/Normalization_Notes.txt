Normalization : generally refeeered to MinMax,Mean normalization , max absolute , robust scaling 
90% = MinMax 

now using train,test split 

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state=0)

X = inputs (such as alochol , malic acid ) , so you can either df.drop("Class",axis = 1) or df.iloc[:,1:3]
Y = output (class) , so df["class"]